tool to static dump keys of ExHIBIT `.rld`  
Usage: drag game exe to the program(make sure `resident.dll` and `ExHIBIT.ini` exists) Then it will gengerate `keys` folder  
test on few games, make issue if the game is not support